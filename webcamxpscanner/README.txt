Make sure you have installed the bs4 and requests libraries using pip 
#pip install bs4
#pip install requests
has two main functions one takes an integer X as input and generates X random ips
and another that takes the ip address as input and appends listofurls with valid WebcamXP urls and then prints them.
